// @ts-nocheck
export const localFooterStyles = ({addComponents}) => {
  addComponents({
    ".local-footer .wysiwyg": {
      h2: {
        fontSize: "1.5em",
      },
      h3: {
        fontSize: "1.3em",
      },
      h4: {
        fontSize: "1.1em",
      },
    },
  })
}
